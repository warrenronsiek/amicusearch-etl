package com.amicusearch.etl.read

import com.amicusearch.etl.AppParams
import org.apache.spark.sql.{DataFrame, SQLContext, SparkSession}

object ReadCourtsDB {

//  def apply(path: String, env: AppParams.Environment.Value)(implicit spark: SparkSession, SQLContext: SQLContext): Unit => DataFrame = {
//    // read json from resources and return a dataframe
//
//
//  }

}
